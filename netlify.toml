[build]
  command = "npm ci && hugo --gc --minify --baseURL $DEPLOY_PRIME_URL/"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.139.4"
  NODE_VERSION = "24"
  HUGO_ENV = "production"
  TZ = "America/Denver"
